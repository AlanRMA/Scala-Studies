object Script {
  def main(args: Array[String]): Unit = {
    def Run(): Unit = {
        // Main Logic
    }
    class Node() {
        // add .next and value arg
        // handle Node(null) cases
    }

    class LinkedList {
      //add head and tail

      def append(value: Any): Unit = {
        // append logic
      }

      def pop(): Any = {
        // pop logic
      }

      def find(value: Any): Node = {
        //find logic
      }

      def show(): Unit = {
        //show logic
      }
    }
    Run()// Output: 4
  }
}